-- PostgreSQL submission
SELECT name FROM Customer
WHERE referee_id != '2' OR referee_id IS null

-- MS SQL Server submission (T-SQL)
SELECT name FROM Customer
WHERE referee_id NOT LIKE '2' OR referee_id IS null
